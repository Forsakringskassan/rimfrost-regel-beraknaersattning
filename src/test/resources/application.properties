#WireMock
quarkus.wiremock.devservices.enabled=true
quarkus.wiremock.devservices.reload=true
quarkus.wiremock.devservices.files-mapping=src/test/resources
quarkus.wiremock.devservices.global-response-templating=false

#Kafka
mp.messaging.incoming.regel-requests.connector=smallrye-in-memory
mp.messaging.outgoing.regel-responses.connector=smallrye-in-memory
quarkus.kafka.devservices.enabled=false

# Arbetsgivare adapter - use WireMock
arbetsgivare.api.base-url=${wiremock.server.url}

# Handlaggning adapter - use WireMock
handlaggning.api.base-url=${wiremock.server.url}

# Regel config path
application.config.path=regel-config.yaml
